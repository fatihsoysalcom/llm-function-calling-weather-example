# llm-function-calling-weather-example
Demonstrates how a Large Language Model (LLM) can be instructed to call external functions based on user queries, using the OpenAI API. It shows the process of defining tools, sending them to the LLM, and executing the function call when the LLM identifies a need.
